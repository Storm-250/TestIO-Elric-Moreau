//
//  QuestionViewModel.swift
//  TestSubDeVinci
//
//  Created by COURS on 19/04/2024.
//


import Foundation
/*
class QuestionViewModel: ObservableObject {
    var model = Model.self
    var received: Bool = false
    @Published var questions: [Question] = []
    
    func getQuesitons() {
        questions = model
    }
    
    private func getIndex(of element: Question) -> Int? {
        for (index, question) in questions.enumerated() {
            print("\(question.statement) se trouve à l'index :\(index)")
            if element.statement == question.statement {
                return index
            }
        }
        return nil
    }
    
}
*/
